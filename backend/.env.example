# Optional. Only needed for the "Generate AI Summary" button (real LLM call via Groq).
# Get a free key at https://console.groq.com
# If left unset, the app still works fully -- that one button will show a friendly
# "not configured" message instead of crashing.
GROQ_API_KEY=
